#pragma once 

#include "tmx/internal/setup.hpp"

namespace tmx
{
    namespace Numbers
    {
        // template<typename T>
        // TMX_INLINE constexpr T min(T a, T b) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T max(T a, T b) noexcept;

        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;

        // template<typename T>
        // TMX_INLINE constexpr T clamp(T val, T min, T max) noexcept;

        // template<typename T>
        // TMX_INLINE constexpr T floor(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T ceil(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T round(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T trunc(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T mod(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;
        // template<typename T>
        // TMX_INLINE constexpr T abs(T val) noexcept;

        
    }
}